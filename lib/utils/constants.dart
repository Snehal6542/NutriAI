import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF00C853);
  static const Color background = Colors.black;
  static const Color card = Color(0xFF1E1E1E); // ✅ change name to card
}